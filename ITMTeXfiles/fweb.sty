% --- SAMPLE STYLE FILE fweb.sty ---

% Typesetting and page layout macros for FWEB sources.

% Pretty up the documentation.
named.preamble "\\7" % Empty line after module name.

% Non-default names for the TeX output files.
index.tex "#.ndx"
modules.tex "#.mds"
contents.tex "#.cts"

% Separate index entries by \letter{...}. See limbo for \letter.
lethead.prefix "\\letter{" 
lethead.suffix "}\n"
lethead.flag -1 % Use lower case letters.

% Subscript symbols with the module number where they were defined.
mark_defined.fcn_name 1
mark_defined.WEB_macro 1
mark_defined.outer_macro 1
mark_defined.exp_type 1
mark_defined.typedef_name 1

% Get LaTeX to include the optional style file eqalign.sty.
LaTeX.options "eqalign"
